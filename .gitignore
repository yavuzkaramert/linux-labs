.DS_Store
*.log
.code-review-graph/

# Per-lab session recordings (history + exit codes) — local debrief material
.sessions/

# Secrets — never commit
.env
.env.*
*.pem
*.key
id_rsa*
id_ed25519*
credentials
.netrc
